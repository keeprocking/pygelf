language: python
python:
  # - "2.6"
  # - "2.7"
  # - "3.3"
  # - "3.4"
  # - "3.5"
  - "3.6"
  # - "pypy"
install:
  - pip install -e .
  - pip install requests
before_script:
  - docker-compose -f tests/config/docker-compose.yml up -d
  - sleep 30
  - curl -v -u admin:admin 'http://127.0.0.1:9000/api/search/universal/relative?query=test&range=5&fields=message'
script: py.test -s -v
after_script:
  - docker-compose down
